                                                            includes() method:

Name: Sneka K

The includes() method returns true if a string contains a specified string.Otherwise it returns false.
The includes() method is case sensitive.

Syntax:
    string.includes(searchvalue, start)

Parameters:
    1. searchvalue--Required.The string to search for.
    2. start--Optional.The position to start from.Default value is 0.

Name: Fathima Saja S
      The includes() method checks a certain value is present in array or not. 
      If the specific value is found, it returns true else returns false. 

Name: Babisha V
       It is used to check whether the certain value is present in an array.
       It returns true or false.
       Syntax:
         includes(searchElement)
         includes(searchElement, fromIndex)
       The index value and the values after the index value is checked for search element,
       Examples:
          const array1 = [1, 2, 3];
          console.log(array1.includes(2));// true
       Condition:
           fromIndex > array length (array is not searched)// false
           computed index=length+index (complete array is checked)
           fromIndex < 0 (fromIndex+array length is used)
           fromIndex>= array length// false
       Example:
           const array1 = [1, 2, 3];//array.length=3
           console.log(array1.includes(2,-2));// true
           //(3+(-2))=1

Name: Haritha Shalu 

        The includes() method checks an element whether the given element is present in the array or not.
        If the element is Present in the array it will return true statement else if will return the false statement.
       
       
                                                                  indexOf() method:
Name: Sneka K
       To find the position of an element in an array, you use the indexOf() method.
This method returns the index of the first occurrence the element that you want to find, 
or -1 if the element is not found.

Synatx:
    Array.indexOf(searchElement, fromIndex)

Parameters:
   1. searchElement--The searchElement argument is the element that you want to find in the array.
   2. fromIndex--The fromIndex is an array index at which the function starts the search.
The fromIndex argument can be a positive or negative integer.In case you omit the fromIndex argument,
 the indexOf() method starts searching from the begining of the string.

Note: the index() of method use the strict equality comparison algorithm that is similar to the 
triple-equals operator (===).

Name: Fathima Saja 
          The indexOf() method gives the first index of the search element present in the array. 
          If the element is not present in the array, it returns -1. When the element is present in
          multiple times in an array, it can be searched from a particular index by indexOf(searchElement, fromIndex).

Name: Haritha Shalu
            The indexOf() method print the index value of an element if that present in the original array as an positive number that start with 0.
            If the assigned element is not present in the array it will return -1 .

Name: Babisha V            
        This method is used to get the first index of the search element.If the search element is not present in an array,it returns -1.
             If present return the index value.
        Syntax:     
             indexOf(searchElement)
             indexOf(searchElement, fromIndex)// fromIndex:default 0    


                                                                 isArray() method:
Name: Sneka K

arr.isArray() method determines whether the value passed to this function is an array or not. 
This function returns true if the argument passed is an array else it returns false.

Syntax:
Array.isArray(obj)

Name: Fathima Saja 
         Array.isArray() method checks if the passed value is present or not. If the passed value is numbers, 
         strings, null, boolean values or undefined values not inside an array will return false.

Name: Babisha V         
         It is used to check whether the passed value is an array or not.
               If the passed value is an array return true else return false.
        Syntax:
           Array.isArray(value);

                                                               Array.prototype.reduce()
Name: Sneka K
       
    The reduce() method process all the element in an array and finaly it will produce the single value.

Synatx: 
    array.reduce(callbackFn[, initialValue])
callbackFn has the following syntax:
    function callbackFn(previousValue,CurrentValue,currentIndex,array){/**/}

Name: Fathima Saja 
           The reduce() method executes a callback function on every elements of an array and it 
           will return one single output value.

Name: Babisha V           
        This method executes a reducer function for array element.It gives single value as output.
    Syntax:
      array.reduce(accumulator,current value);
                                                               
